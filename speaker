import pyttsx3

engine = pyttsx3.init()

def speak(text):
    engine.say(text)
    engine.runAndWait()

    import webbrowser
import os
import pyttsx3
import webbrowser

engine = pyttsx3.init()
def speak(text):
    engine.say(text)
    engine.runAndWait()

print("hey there my name is jarvis your assistant how can i help you?")
speak("Hey there, I am Jarvis, how can I help you?")

while True:
    query = input("enter your query: ").lower().strip()

    if "google" in query:
        speak("Opening Google")
        webbrowser.open("https://www.google.com")

    if "open my github profile" in query:
        speak("Opening your GitHub profile")
        webbrowser.open("https://github.com/virensingh011")

    if "open deapseek" in query:
        speak("Opening Deapseek")
        webbrowser.open("https://www.deapseek.com")

    elif "youtube" in query:
        speak("Opening YouTube")
        webbrowser.open("https://www.youtube.com")

    elif "spotify" in query:
        speak("Opening Spotify")
        os.system("start spotify")

    elif "github" in query:
        speak("Opening GitHub")
        webbrowser.open("https://www.github.com")
    elif "settings" in query:
        speak("Opening Settings")
        os.system("start ms-settings:")

    elif "exit" in query:
        speak("Goodbye boss")
        break
    elif " calculation in query":
        speak("What calculation do you want to perform?")
        print("enter your first number:")
        first_number = float(input())
        print("enter your second number:")
        second_number = float(input())
        print("enter the operator (+, -, *, /):")
        operator = input()

        if operator == "+":
            result = first_number + second_number
        elif operator == "-":
            result = first_number - second_number
        elif operator == "*":
            result = first_number * second_number
        elif operator == "/":
            result = first_number / second_number

        speak(f"The result is {result}")
        print(f"The result is: {result}")
    elif "news" in query:
        speak("Opening news updates")
        webbrowser.open("https://www.bbc.com/news")
    elif "more professional talk" in query:
        speak("Sure, I can talk more professionally. How can I assist you today?")
        speak("What would you like to do? You can ask me to open websites, perform calculations, or get news updates or would u like to talk with chatgpt my friend yes or no.")
    if "yes" in query:
        speak("Opening ChatGPT")
        webbrowser.open("https://chat.openai.com")
    else:
        speak("Sorry, I did not understand that do u want to do other functions yes or no")
        if "yes" in query:
            speak("What do you want to do?")
        print("you typed:", query)   # debug line (you can remove later)
        query = input("enter your query: ").lower().strip()
        if "calculation" in query:
            speak("What calculation do you want to perform?")
            print("enter your first number:")
            first_number = float(input())
            print("enter your second number:")
            second_number = float(input())
            print("enter the operator (+, -, *, /):")
            operator = input()

            if operator == "+":
                result = first_number + second_number
            elif operator == "-":
                result = first_number - second_number
            elif operator == "*":
                result = first_number * second_number
            elif operator == "/":
                result = first_number / second_number

            speak(f"The result is {result}")
            print(f"The result is: {result}")

        if "no" in query:
            speak("Okay, have a nice day would you like to perform more calculations? yes or no")
            if 'yes' in query:
                speak("What calculation do you want to perform?")
                print("enter your first number:")
                first_number = float(input())
                print("enter your second number:")
                second_number = float(input())
                print("enter the operator (+, -, *, /):")
                operator = input()

                if operator == "+":
                    result = first_number + second_number
                elif operator == "-":
                    result = first_number - second_number
                elif operator == "*":
                    result = first_number * second_number
                elif operator == "/":
                    result = first_number / second_number

                speak(f"The result is {result}")
                print(f"The result is: {result}")
            elif 'no' in query:
                speak("Okay, have a nice day would you like to get some news updates? yes or no")
                if 'yes' in query:
                    speak("Opening news updates")
                    webbrowser.open("https://www.bbc.com/news")
                elif 'no' in query:
                    speak("Okay, have a nice day boss")

            break